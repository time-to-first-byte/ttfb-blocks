const attributes = {
    alignment: {
        type: 'string',
    },
    backgroundColor: {
        type: 'string',
        default: 'rgb(142, 209, 252)',
    },
    textColor: {
        type: 'string',
    },
    iconName: {
        type: 'string',
        default: 'fas fa-lightbulb fa-2x',
    },
};

export default attributes;
